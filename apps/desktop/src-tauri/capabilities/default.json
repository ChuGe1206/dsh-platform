{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default capability for the main window: core events/window, shell notification, dialog and global-shortcut plugins. App-defined (generate_handler!) commands are always allowed.",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "notification:default",
    "dialog:default",
    "global-shortcut:default"
  ]
}
