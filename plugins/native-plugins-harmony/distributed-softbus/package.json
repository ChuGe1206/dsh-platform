{
  "name": "@dsh-platform/native-distributed-softbus",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Distributed softbus (nearby device discovery/messaging) surface for HarmonyOS shells.",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": { ".": "./src/index.ts" },
  "scripts": {
    "build": "tsc --noEmit -p tsconfig.json",
    "typecheck": "tsc --noEmit -p tsconfig.json"
  },
  "dependencies": {
    "@dsh-platform/native-harmony-bridge": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5.8.0"
  }
}
