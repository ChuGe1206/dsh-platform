{
  "name": "@dsh-platform/native-harmony-bridge",
  "version": "0.1.1",
  "private": true,
  "type": "module",
  "description": "JS side of the HarmonyOS ArkWeb bridge (call native capabilities, receive push/events).",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "build": "tsc --noEmit -p tsconfig.json",
    "typecheck": "tsc --noEmit -p tsconfig.json"
  },
  "dependencies": {
    "@dsh-platform/shared-bridge": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5.8.0"
  }
}
