{
  "name": "@dsh-platform/native-file-access",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "File access (pick/save/open) for the HarmonyOS shell through HarmonyBridge.",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": { ".": "./src/index.ts" },
  "scripts": {
    "build": "tsc --noEmit -p tsconfig.json",
    "typecheck": "tsc --noEmit -p tsconfig.json"
  },
  "dependencies": {
    "@dsh-platform/native-harmony-bridge": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5.8.0"
  }
}
